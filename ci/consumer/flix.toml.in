[package]
name        = "consumer"
description = "A throwaway project that only checks that logfx can be pulled in"
version     = "0.0.0"
flix        = "@FLIX@"
authors     = ["ci"]

[dependencies]
"github:ababup1192/logfx" = { version = "@VERSION@", security = "unrestricted" }

[mvn-dependencies]
